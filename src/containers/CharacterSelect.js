import React, { Component } from 'react';
import { Text } from 'react-native';

class CharacterSelect extends Component {
    render() {
        return (
            <Text>CharactetSelect</Text>
        );
    }
}

export default CharacterSelect;